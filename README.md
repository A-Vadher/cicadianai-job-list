# Circadianai job list

A functional browser prototype for assigning paid projects, accepting work, uploading evidence, approving jobs, and viewing a private earnings ledger.

## Run it

Open `index.html` in a modern web browser. No installation is required.

## Demo flow

1. Create a **Host** account and add a project. Set its hours, hourly rate, optional calendar deadline, and whether it is open to anyone or a specific worker.
2. Create a **Worker** account (sign out first). The worker can accept available work, attach photos or common document, spreadsheet, presentation, audio, video and ZIP evidence files, or request cancellation.
3. Sign back in as the host and approve a submitted project.
4. The worker’s **My earnings** page then shows the approved payment, monthly summary, and year chart.

## Important production note

This version deliberately uses browser-local storage for a zero-setup demo. Real shared accounts, secure passwords, role enforcement, and cross-device evidence uploads require a server backend and object storage. Do not use this prototype for real payroll data.

## GitHub

This folder is ready to put in a GitHub repository. After Git is available on this Mac, initialize or connect a repository and push the files. Git operations are currently blocked because macOS requires accepting the Xcode license.
